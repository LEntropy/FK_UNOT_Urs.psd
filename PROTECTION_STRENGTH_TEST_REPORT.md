# 이미지 보호강도 테스트 진행 기록

작성일: 2026-07-24  
주 작업 저장소: `work/FK_UNOT_Urs.psd-main`  
실험 코드 위치: `work/FK_UNOT_Urs.psd-main/apps/protection-svc/ml-engine/experiments`  
비교 이미지 위치: `outputs`

## 1. 테스트 목적

강한 이미지 보호 효과를 확보하면서도 사람이 보기에 원본 그림이 훼손되지 않는 조건을 찾는 것이 목적이다.

판정 기준은 다음 두 가지다.

1. **보호 효과**: 스타일 학습·편집·LoRA 학습 등에 원본 특징이 그대로 이용되지 않도록 방해하는가?
2. **원본 보존**: 색, 배경, 선, 질감, 인물 형태가 육안상 자연스럽게 유지되는가?

자동 지표만으로는 실제 훼손 정도를 판단하기 어려우므로, 각 실험 결과를 비교 이미지로 만든 뒤 사용자가 직접 육안 판정하는 방식으로 진행했다.

## 2. 사용 이미지

처음 전달받은 5장의 테스트 이미지는 아래 위치에 모두 보존되어 있다.

`work/FK_UNOT_Urs.psd-main/apps/protection-svc/ml-engine/experiments/five-image-assets`

- `art2.jpg`
- `art7.jpg`
- `art8.jpg`
- `art16.jpg`
- `art24.jpg`

누락된 원본은 없다.

다만 지금까지 실제 보호 결과가 생성된 범위는 다음과 같다.

- 초기 epsilon 비교: `art2`, `art7`, `art8`
- VGG 계층·주파수·마스크 비교: `art2`
- `art16`, `art24`: 보호 결과 미생성
- 일부 실험에서 `art24`는 보호할 원본이 아니라 스타일/디코이 타깃으로 지정됨

## 3. 보호강도 관련 주요 변수

| 변수 | 의미 | 값이 커질 때의 일반적 영향 |
|---|---|---|
| `epsilon` | 원본 픽셀에서 허용하는 최대 교란 범위 | 보호 여지는 커지지만 노이즈와 색 변화가 보이기 쉬움 |
| `steps` | 교란을 최적화하는 반복 횟수 | 목표에 더 수렴할 수 있지만 시간 증가 및 훼손 누적 가능 |
| `lr` | 한 단계에서 교란을 갱신하는 크기 | 너무 크면 불안정하고 너무 작으면 제한된 단계 안에 수렴하기 어려움 |
| `color_weight` | 저주파 색·명암 변화를 억제하는 손실 가중치 | 전체 색 틀어짐을 줄이지만 보호 최적화 자유도를 제한할 수 있음 |
| `mask_low` | 평탄한 영역에 허용하는 epsilon 배율의 하한 | 낮추면 하늘·피부·단색 배경의 노이즈를 더 억제 |
| `mask_high` | 질감·경계 영역에 허용하는 epsilon 배율의 상한 | 높이면 복잡한 영역에 교란을 더 집중 |
| perceptual mask | 평탄한 곳은 약하게, 질감과 경계는 강하게 보호하도록 공간별 예산을 분배 | 같은 epsilon에서도 체감 훼손을 줄일 가능성 |
| EOT | 리사이즈 등 변환을 거친 뒤에도 교란 효과가 유지되도록 최적화 | 실사용 내구성이 좋아질 수 있지만 연산량 증가 |
| `eot_samples` | 단계마다 함께 계산할 변환 표본 수 | 많을수록 변환 대응 범위와 연산량 증가 |
| `clip_transfer_weight` | CLIP 특징을 디코이 타깃 쪽으로 이동시키는 손실 가중치 | 편집·전이 공격 방어를 노리지만 과하면 의미 특징이나 화질에 영향 가능 |
| VGG layer weights | 얕은/중간/깊은 특징층 중 어디의 스타일 교란을 중시할지 결정 | 계층별로 색·질감·구조에 미치는 양상이 달라짐 |
| frequency placement | 교란을 저·중·고주파 중 어느 대역에 배치할지 결정 | 저주파는 얼룩/색 변화, 고주파는 입자/노이즈로 보일 수 있음 |

## 4. 저장소의 현재 공식 프리셋

`src/style_cloak.py` 기준 프리셋은 다음과 같다.

| 프리셋 | epsilon | steps | color_weight | mask_low | mask_high | clip_transfer_weight |
|---|---:|---:|---:|---:|---:|---:|
| L1 Preview | 0.02 | 150 | 0 | 0.30 | 1.70 | 0.1 |
| L2 Portfolio | 0.03 | 300 | 8 | 0.30 | 1.70 | 0.5 |
| L3 Anti-Train | 0.05 | 500 | 8 | 0.15 | 1.70 | 1.0 |

L2/L3는 오케스트레이션 경로에서 perceptual mask와 EOT를 사용한다. 현재 서비스 처리 방식은 원본 비율을 유지하고 긴 변을 최대 1024px로 처리한 뒤 원본 해상도로 복원하는 구조다.

## 5. 지금까지 수행한 실험

### 5.1 초기 epsilon 비교

공통 탐색 조건:

- 처리 크기: 256px
- steps: 60
- `color_weight=8`
- perceptual mask 활성
- `mask_low=0.15`, `mask_high=1.70`
- EOT 활성
- `clip_transfer_weight=0`
- epsilon 후보: 0.02, 0.03, 0.04

생성 범위:

- `art2`: epsilon 0.02 / 0.03 / 0.04
- `art7`: epsilon 0.02 / 0.03 / 0.04
- `art8`: epsilon 0.02만 생성
- `art16`, `art24`: 미실행

사용자 육안 판정에서는 한 비교 결과에서 **세 번째 이미지가 가장 원본과 비슷하다**는 의견이 있었다. 다만 대화 기록만으로는 그 이미지가 정확히 어느 파일·후보였는지 확정할 수 없어 특정 epsilon의 통과 판정으로 기록하지 않는다.

이 단계는 공식 프리셋 검증이 아니라 빠른 탐색용 축소 실험이었다.

### 5.2 VGG 스타일 계층 가중치 비교

대상: `art2`

공통 조건:

- epsilon 0.03
- steps 60
- 256px
- 고정 시드 42 결과 사용

후보:

- `uniform`: 모든 계층 동일 가중치
- `shallow`: 얕은 계층 강조
- `middle`: 중간 계층 강조
- `deep`: 깊은 계층 강조
- `middle_deep`: 중간·깊은 계층 혼합

자동 측정에서 스타일 드리프트가 가장 컸던 후보는 `shallow`(0.0994)였고, PSNR은 후보별 약 34dB로 비슷했다.

사용자 육안 판정:

- 비교 결과가 전반적으로 훼손되어 채택하지 않음
- 자동 지표가 비슷해도 실제 배경과 화면 질감의 이질감은 크게 느껴짐

비교 이미지:

- `outputs/vgg-layer-weight-comparison-art2-equal-resolution.jpg`
- `outputs/vgg-layer-weight-difference-4x-art2.jpg`

### 5.3 교란 주파수 대역 비교

대상: `art2`

공통 조건:

- epsilon 0.03
- steps 60
- 256px

후보:

- `none`: 별도 주파수 투영 없음
- `low`: 저주파 중심
- `mid`: 중주파 중심
- `high`: 고주파 중심

자동 측정:

| 후보 | 스타일 드리프트 | PSNR |
|---|---:|---:|
| none | 0.0843 | 34.01dB |
| low | 0.0050 | 33.41dB |
| mid | 0.0455 | 39.01dB |
| high | 0.0644 | 36.17dB |

`low`는 훼손 부담이 있는데도 보호 효과 지표가 매우 낮아 특히 비효율적이었다.

사용자 육안 판정:

- 모든 후보에서 이미지가 깨져 보임
- 채택 후보 없음

비교 이미지:

- `outputs/frequency-placement-comparison-art2.jpg`

### 5.4 주파수 혼합 비교

대상: `art2`

후보:

- 저주파 75% + 고주파 25%
- 저주파 50% + 고주파 50%
- 저주파 25% + 고주파 75%
- 중주파 50% + 고주파 50%

자동 지표상 `mid50_high50`의 스타일 드리프트가 0.0794로 가장 컸지만, 사용자 육안 판정에서는 모두 이미지가 깨져 채택하지 않았다.

비교 이미지:

- `outputs/frequency-mix-comparison-art2.jpg`

### 5.5 영역별 perceptual mask 비교

대상: `art2`

후보:

| 후보 | mask_low | mask_high | 스타일 드리프트 | PSNR |
|---|---:|---:|---:|---:|
| current_015_170 | 0.15 | 1.70 | 0.0843 | 34.01dB |
| flat_strict_005_170 | 0.05 | 1.70 | 0.0816 | 34.34dB |
| flat_strict_capped_005_120 | 0.05 | 1.20 | 0.0717 | 36.89dB |
| edge_only_000_170 | 0.00 | 1.70 | 0.0805 | 34.51dB |
| edge_only_capped_000_100 | 0.00 | 1.00 | 0.0643 | 38.71dB |

상한을 낮추면 PSNR은 좋아졌지만 스타일 드리프트도 함께 감소했다.

사용자 육안 판정:

- 모든 후보가 깨져 보임
- 채택 후보 없음

비교 이미지:

- `outputs/region-mask-comparison-art2.jpg`

## 6. 왜 결과가 전반적으로 깨져 보였는가

현재 결과만으로 “다른 보호강도 유형은 원래 모두 잘 깨진다”고 결론 내릴 수 없다. 이번 실험 방식에 다음 문제가 섞여 있었다.

1. **축소 탐색 결과를 확대해 비교함**  
   256px에서 보호를 계산한 뒤 원본 크기 수준으로 확대하면 보호 노이즈뿐 아니라 리사이즈 흔적도 함께 커진다.

2. **공식 프리셋과 다른 단축 조건을 사용함**  
   대부분 60 steps였고 CLIP 전이 손실도 꺼져 있었다. 공식 L2의 300 steps, 1024px, `clip_transfer_weight=0.5`, 서비스 처리 경로와 동일한 결과가 아니다.

3. **변수를 분리해 본 탐색 실험임**  
   VGG 계층, 주파수 투영, 마스크 범위를 의도적으로 크게 바꿔 각각의 영향을 확인했다. 실제 최종 후보처럼 균형을 맞춘 설정은 아니다.

4. **자동 지표와 사람의 시각이 일치하지 않음**  
   PSNR이나 특징 유사도가 양호해도 배경의 얼룩, 선 주변 노이즈, 색 이질감은 사람이 즉시 발견할 수 있다.

5. **일부 초기 통계의 비교 기준에 주의가 필요함**  
   초기 결과 JSON의 `meanAbsoluteError255`와 채널 평균 이동량은 letterbox/크기 복원 과정의 영향을 포함했을 가능성이 있어 최종 화질 판정값으로 사용하지 않는 것이 안전하다.

따라서 지금까지의 실패는 보호 방식 자체가 모두 불가능하다는 뜻이 아니라, **축소·단축 탐색 결과가 최종 화질 판단용으로 부적절했다는 신호**로 보는 편이 정확하다.

## 7. 준비했지만 실행하지 않은 공식 L2 재검증

탐색 실험의 문제를 제거하기 위해 `art2` 한 장에 저장소 공식 L2 조건을 그대로 적용하는 스크립트를 준비했다.

파일:

`experiments/run_official_l2_art2.py`

예정 조건:

- `epsilon=0.03`
- `steps=300`
- `color_weight=8`
- `mask_low=0.30`, `mask_high=1.70`
- perceptual mask 활성
- EOT 활성, `eot_samples=1`
- `clip_transfer_weight=0.5`
- 최대 1024px 처리
- 원본 비율 유지
- 처리 후 원본 해상도 복원
- 주파수 강제 투영 없음

필요 환경도 준비했다.

- `open_clip_torch` 설치 완료
- CLIP ViT-B-32 OpenAI 가중치 다운로드 완료
- `super-image` 설치 완료
- VGG19 캐시 준비

그러나 CPU에서 한 장당 약 2~4시간이 예상되어 실행 전에 중단했다. 따라서 `outputs/official-l2-art2`에는 완료된 공식 L2 결과가 없으며, 공식 L2 화질이나 보호 성공 여부도 아직 판정하지 않았다.

## 8. 현재 결론

- 5장 원본은 모두 보존되어 있다.
- 현재까지의 완료 결과는 대부분 256px·60 steps의 탐색 실험이다.
- VGG 계층 조정, 주파수 배치/혼합, 마스크 범위 조정은 `art2` 육안 검사에서 모두 채택되지 않았다.
- 이 결과를 공식 L2 실패로 해석하면 안 된다.
- 공식 1024px L2와 LoRA 공격 검증은 아직 수행되지 않았다.
- 다음에 재개한다면 먼저 `art2` 한 장만 공식 L2로 처리하고, 사용자가 원본과 직접 비교한 뒤 나머지 4장 실행 여부를 결정하는 것이 적절하다.

## 9. 관련 결과 파일

### 비교 이미지

- `outputs/vgg-layer-weight-comparison-art2-equal-resolution.jpg`
- `outputs/vgg-layer-weight-difference-4x-art2.jpg`
- `outputs/frequency-placement-comparison-art2.jpg`
- `outputs/frequency-mix-comparison-art2.jpg`
- `outputs/region-mask-comparison-art2.jpg`

### 세부 수치

- `experiments/five-image-results/*/results.json`
- `experiments/layer-weight-results-seed42/art2/results.json`
- `experiments/frequency-results/art2/results.json`
- `experiments/frequency-mix-results/art2/results.json`
- `experiments/region-mask-results/art2/results.json`

## 10. 용어상 주의

이 기록에서 “보호 실패”는 두 경우를 구분해야 한다.

- **보호 효과 실패**: 공격이나 학습을 실질적으로 방해하지 못해 보호강도가 의미 없는 경우
- **화질 실패**: 보호 효과와 별개로 원본이 사람이 보기 어려울 정도로 훼손된 경우

지금까지 사용자가 직접 판정한 실패는 주로 **화질 실패**다. 실제 LoRA 공격 실험을 하지 않았으므로 각 후보의 최종 보호 효과 실패 여부는 아직 확정할 수 없다.

## 11. 최초 전달 자료의 별도 보호 파이프라인

최초 `laptop-protection-strength-test.zip`과 인수인계 문서에는 FK 저장소의 Style Cloak 외에 별도의 통합 보호 파이프라인이 포함되어 있었다.

주요 구성:

- **P2T (Perceptual-Preserving Transform)**: 위상, 잔차, 국소 대비, 경계 성분을 이용해 시각적 훼손을 억제하면서 변환을 추가
- **SCL**: 스타일 특징 학습을 방해하기 위한 최적화 계층
- **CML**: 개념 정렬 또는 개념 학습을 방해하기 위한 계층
- **TL**: 전달 자료에 포함된 추가 보호 계층

전달 자료의 원래 방향은 서비스의 기존 L2 보호 위에 P2T 계열 보호를 추가했을 때, 원본의 시각적 자연스러움을 유지하면서 학습 방어가 더 강해지는지 확인하는 것이었다.

이 P2T/SCL/CML 수치는 FK 저장소의 아래 변수와 이름이나 값이 1:1로 대응하지 않는다.

- `epsilon`
- `steps`
- `color_weight`
- `mask_low`
- `clip_transfer_weight`
- EOT

두 파이프라인은 알고리즘과 손실 함수가 다르므로, 예를 들어 P2T의 phase 값과 Style Cloak의 epsilon을 같은 보호강도 단위로 비교하면 안 된다.

### 전달 자료의 L2 기준

| 계층 | 값 |
|---|---:|
| P2T phase | 0.035 |
| P2T residual | 0.035 |
| P2T local contrast | 0.018 |
| P2T edge | 0.012 |
| SCL steps | 8 |
| SCL epsilon | 0.015 |
| CML steps | 8 |
| CML epsilon | 0.015 |

### 전달 자료의 L2+ 1단계

| 계층 | 값 | L2 대비 |
|---|---:|---:|
| P2T phase | 0.040 | +0.005 |
| P2T residual | 0.040 | +0.005 |
| P2T local contrast | 0.020 | +0.002 |
| P2T edge | 0.014 | +0.002 |
| SCL steps | 10 | +2 |
| SCL epsilon | 0.017 | +0.002 |
| CML steps | 10 | +2 |
| CML epsilon | 0.017 | +0.002 |

### 전달 자료의 L2+ 2단계

| 계층 | 값 | L2 대비 |
|---|---:|---:|
| P2T phase | 0.042 | +0.007 |
| P2T residual | 0.042 | +0.007 |
| P2T local contrast | 0.021 | +0.003 |
| P2T edge | 0.015 | +0.003 |
| SCL steps | 11 | +3 |
| SCL epsilon | 0.018 | +0.003 |
| CML steps | 11 | +3 |
| CML epsilon | 0.018 | +0.003 |

전달 기록상 L3 Anti-Train과 L3 Balanced Candidate는 평탄한 영역에서 노이즈 또는 이질감이 보여 탈락한 상태였다. 따라서 이 자료에서도 숫자 지표가 좋아도 사용자가 이질감을 발견하면 후보를 탈락시키는 원칙을 사용했다.

## 12. FK 저장소에서 수행한 초기 128px 탐색

본격적인 256px 비교 전에 `art2` 계열 원본과 스타일 타깃을 이용한 128px·20 steps 빠른 스윕도 수행했다.

| 후보 | epsilon | 색 억제 | 마스크 | EOT | 스타일 드리프트 | PSNR |
|---|---:|---:|---:|---:|---:|---:|
| eps010_plain | 0.01 | 꺼짐 | 꺼짐 | 꺼짐 | 0.0899 | 39.27dB |
| eps020_plain | 0.02 | 꺼짐 | 꺼짐 | 꺼짐 | 0.1151 | 35.20dB |
| eps030_plain | 0.03 | 꺼짐 | 꺼짐 | 꺼짐 | 0.1328 | 31.73dB |
| eps050_plain | 0.05 | 꺼짐 | 꺼짐 | 꺼짐 | 0.1478 | 28.57dB |
| eps080_failure | 0.08 | 꺼짐 | 꺼짐 | 꺼짐 | 0.1557 | 26.53dB |
| eps050_color8 | 0.05 | 8 | 꺼짐 | 꺼짐 | 0.1479 | 28.58dB |
| eps050_mask015 | 0.05 | 8 | 켜짐 | 꺼짐 | 0.1462 | 29.61dB |
| eps030_mask015_eot | 0.03 | 8 | 켜짐 | 켜짐 | 0.1145 | 32.87dB |

이 탐색에서 확인된 경향:

- epsilon을 높일수록 스타일 드리프트는 증가했지만 PSNR은 계속 하락했다.
- epsilon 0.08은 보호 지표 상승폭에 비해 화질 손실이 커 실패 기준 후보로 분류했다.
- epsilon 0.05에서 `color_weight=8`만 추가한 결과는 plain과 거의 같아, 색 손실 하나만으로 훼손 문제를 해결하지 못했다.
- perceptual mask를 추가하면 epsilon 0.05에서 PSNR이 약 1dB 개선됐지만 근본적인 훼손 문제는 남았다.
- epsilon 0.03 + mask + EOT는 강도와 화질 사이의 절충 후보가 되어 다음 256px 실험으로 넘어갔다.

결과 위치:

- `experiments/strength-results-20/results.json`
- `experiments/strength-results-20/comparison-grid.jpg`
- `experiments/strength-results-20/comparison-grid-cropped.jpg`

이 단계 역시 매우 낮은 해상도에서 방향만 보기 위한 실험이므로 최종 화질 검증으로 취급하지 않는다.

## 13. 256px epsilon 탐색의 수치 상세

### art2

| epsilon | 스타일 드리프트 | 원본 스타일 유사도 | PSNR | RMSE |
|---:|---:|---:|---:|---:|
| 0.02 | 0.0716 | 0.9570 | 37.05dB | 0.0140 |
| 0.03 | 0.0826 | 0.9431 | 34.00dB | 0.0200 |
| 0.04 | 0.0940 | 0.9273 | 31.84dB | 0.0256 |

### art7

| epsilon | 스타일 드리프트 | 원본 스타일 유사도 | PSNR | RMSE |
|---:|---:|---:|---:|---:|
| 0.02 | 0.0656 | 0.9597 | 39.23dB | 0.0109 |
| 0.03 | 0.0773 | 0.9388 | 36.14dB | 0.0156 |
| 0.04 | 0.0795 | 0.9323 | 33.98dB | 0.0200 |

### art8

| epsilon | 스타일 드리프트 | 원본 스타일 유사도 | PSNR | RMSE |
|---:|---:|---:|---:|---:|
| 0.02 | 0.0214 | 0.9855 | 37.29dB | 0.0137 |

수치상 같은 epsilon도 이미지마다 효과가 크게 달랐다. 특히 art8의 epsilon 0.02는 art2·art7보다 스타일 드리프트가 훨씬 작았다. 이는 하나의 전역 프리셋이 모든 그림에서 같은 보호 효과와 같은 화질을 보장하지 않는다는 근거다.

## 14. 작업 과정의 시간순 기록

1. 최초 보호강도 테스트 ZIP과 인수인계 문서를 확인했다.
2. 5장 원본을 작업 폴더로 옮기고 누락 여부를 확인했다.
3. 최초 P2T/SCL/CML 테스트와 FK 저장소의 Style Cloak이 서로 다른 파이프라인임을 확인했다.
4. 사용자는 단순 L2 반복이 아니라 `epsilon`, `steps`, `color_weight`, 마스크, CLIP 전이 가중치 외의 다양한 보호 변수도 알고 싶다고 명확히 했다.
5. 한꺼번에 여러 변수를 섞기보다 한 유형씩 결과를 만들어 사용자가 직접 확인하는 방식으로 합의했다.
6. 128px 빠른 epsilon 스윕을 수행했다.
7. 256px에서 epsilon 0.02/0.03/0.04 후보를 비교했다.
8. 사용자는 일부 비교에서 세 번째 이미지가 원본과 가장 비슷하다고 판정했다.
9. VGG 계층 가중치를 개별 유형으로 비교했으나 전체적으로 화질이 나빠 탈락했다.
10. 저·중·고주파 배치를 비교했으나 모두 깨져 보인다는 판정을 받았다.
11. 주파수 혼합 후보도 같은 이유로 탈락했다.
12. 영역 마스크 강도를 비교했으나 모든 후보가 깨져 보인다는 판정을 받았다.
13. 축소 실험 자체가 화질 판단을 왜곡할 가능성을 인정하고, 임의 단축 조건을 중단하기로 했다.
14. 공식 L2 조건을 그대로 적용하는 `art2` 고해상도 기준 테스트를 준비했다.
15. CLIP과 관련 패키지 및 모델 가중치를 설치했다.
16. CPU 예상 시간이 한 장당 2~4시간임을 확인한 뒤, 사용자의 요청에 따라 실행하지 않고 현재 기록 문서를 작성했다.

## 15. 코드 및 환경 변경 사항

### 새로 추가된 실험 스크립트

- `experiments/strength_sweep.py`
- `experiments/layer_weight_sweep.py`
- `experiments/frequency_sweep.py`
- `experiments/region_mask_sweep.py`
- `experiments/run_official_l2_art2.py`

### `src/style_cloak.py`의 실험용 변경

주파수 배치 실험을 위해 다음 기능이 추가됐다.

- 선택적 `frequency_mode`
- 교란 주파수 투영 함수 `project_delta_frequency`
- CLI의 실험용 주파수 모드 인자

기본값은 `none`이므로 주파수 모드를 지정하지 않은 공식 프리셋의 기본 동작은 바뀌지 않는다. 다만 저장소 원본 대비 수정된 코드이므로 향후 정식 반영 여부를 별도로 결정해야 한다.

### 준비된 로컬 환경

- 가상환경: `work/v`
- 모델 캐시: `work/model-cache`
- Hugging Face 캐시: `work/hf-cache`
- 실행 장치 확인 결과: CPU, CUDA 사용 불가
- 설치/준비 완료: PyTorch, torchvision, transformers, `open_clip_torch`, `super-image`, VGG19, CLIP ViT-B-32 OpenAI weights

## 16. 아직 하지 않은 작업

- 공식 L2 조건의 `art2` 결과 생성
- 공식 L2의 나머지 4장 결과 생성
- L1/L2/L3를 같은 처리 경로와 원본 해상도에서 비교
- `steps`만 독립적으로 바꾼 정식 고해상도 테스트
- `color_weight`만 독립적으로 바꾼 정식 고해상도 테스트
- `clip_transfer_weight`만 독립적으로 바꾼 정식 고해상도 테스트
- EOT on/off 및 표본 수 비교
- 마스크 하한·상한의 정식 고해상도 재검증
- LPIPS/DISTS 등 사람의 지각에 더 가까운 화질 평가 추가
- 실제 LoRA 학습 공격
- 보호본으로 학습한 LoRA와 원본으로 학습한 LoRA의 생성 결과 비교
- 리사이즈, JPEG 압축, 크롭, 스크린샷 후에도 보호가 유지되는지 검증

## 17. 권장 후속 테스트 설계

다음 작업은 아래 순서가 적절하다.

### 1단계: 기준선 확정

`art2` 한 장에 공식 L2를 정확히 적용한다. 이때 1024px, 300 steps, EOT, perceptual mask, CLIP 손실과 원본 해상도 복원을 모두 유지한다.

### 2단계: 사용자 화질 판정

원본과 보호본을 동일 해상도로 나란히 보여주고 다음 항목을 확인한다.

- 평탄한 배경의 얼룩
- 얼굴과 피부의 입자
- 선화 주변의 테두리
- 전체 색조 이동
- 세부 묘사의 손실
- 축소 화면과 100% 확대 화면에서의 차이

여기서 화질이 실패하면 나머지 4장을 돌리지 않는다.

### 3단계: 한 변수씩 변경

공식 L2를 기준으로 한 번에 하나만 변경한다.

권장 우선순위:

1. `mask_low`와 `mask_high`
2. `color_weight`
3. `clip_transfer_weight`
4. EOT 범위
5. `steps`

epsilon은 이미 초기 탐색에서 보호 효과와 화질 사이의 지배적인 변수로 확인했으므로, 우선 0.03에 고정하고 다른 변수를 검증한다.

### 4단계: 5장 일반화 확인

art2에서 통과한 후보만 5장 전체에 적용한다. 이미지별로 결과가 다르므로 다섯 장 중 최악의 화질 사례와 최약의 보호 사례를 모두 찾는다.

최악 사례를 선정하는 이유는 평균이 좋아도 특정 그림에서 배경이나 얼굴이 크게 깨지면 배포 프리셋으로 사용할 수 없기 때문이다.

### 5단계: 실제 공격 검증

화질을 통과한 소수 후보만 LoRA 공격에 사용한다.

비교군:

- 원본 5장으로 학습한 LoRA
- 공식 L2 5장으로 학습한 LoRA
- 개선 후보 5장으로 학습한 LoRA

생성 결과에서 인물/스타일 재현도, 프롬프트 일치도, 시각적 붕괴 정도를 같은 seed와 조건으로 비교한다.

## 18. 최종 판정표 양식

향후 결과는 다음 형식으로 누적하는 것이 좋다.

| 후보 | 이미지 | 배경 | 얼굴/피부 | 선화 | 색상 | 확대 노이즈 | 보호 지표 | LoRA 공격 | 판정 |
|---|---|---|---|---|---|---|---:|---|---|
| 공식 L2 | art2 | 미검증 | 미검증 | 미검증 | 미검증 | 미검증 | 미검증 | 미검증 | 대기 |

판정 권장값:

- `통과`: 육안 품질과 공격 방어가 모두 기준 충족
- `화질 실패`: 원본 훼손이 허용 범위를 초과
- `보호 실패`: 화질은 괜찮지만 공격 방어 효과가 부족
- `재검증`: 지표와 육안 결과가 충돌하거나 실행 조건에 문제가 있음

# Slim on purpose: no torch/torchvision here (that's ml-engine's job).
# perceptual-hash comparison reuses the same imagehash-based approach as
# apps/protection-svc/ml-engine/src/perceptual_hash.py, but this service
# imports that module directly rather than depending on ml-engine's full
# (torch-heavy) requirements.txt.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
ImageHash>=4.3
Pillow>=10.0
google-cloud-vision>=3.7
fpdf2>=2.7
playwright>=1.44
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21

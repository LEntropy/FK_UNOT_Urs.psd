# Copy to remote.env and fill in your GPU PC's details.
# remote.env is gitignored — it's machine-specific, not shared.

GPU_HOST=192.168.0.42
GPU_USER=your-windows-username
# Where the project gets copied to on the GPU PC.
GPU_REMOTE_DIR=C:/dontai-ml-engine
SSH_KEY=~/.ssh/dontai_gpu_pc

torch>=2.2
torchvision>=0.17
open_clip_torch>=2.24  # concept_misalign.py's CLIP image encoder (PHASE4_SCOPING.md §1)
super-image>=0.1.7  # upscale.py's EDSR super-resolution model, restores resolution after cloak()'s size-256 processing
Pillow>=10.0
numpy>=1.26
ImageHash>=4.3
scipy>=1.11
pycryptodome>=3.20  # Keccak-256 for orchestrate.py's metadataHash (must match blockchain-svc's ethers.js keccak256, NOT NIST SHA3)
fastapi>=0.110
uvicorn[standard]>=0.29

# server.py's test suite (apps/protection-svc/test/) -- mocks orchestrate.protect(),
# no torch/GPU actually exercised by the tests themselves.
httpx>=0.27
pytest>=8.0

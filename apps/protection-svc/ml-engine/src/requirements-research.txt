# Pins for GPU-pod research scripts under this directory (native_lowfreq_attack.py,
# protection_score.py, caption_image.py, etc.) -- NOT part of the production
# service, which never trains a real LoRA (see ../requirements.txt).
#
# WHY PINNED (2026-08-09): a fresh `pip install diffusers transformers ...`
# with no version constraint pulled transformers==5.14.1, whose
# CLIPModel.get_image_features() started returning a
# BaseModelOutputWithPooling instead of a plain tensor, breaking
# protection_score.py's clip_similarity. Pinning transformers down to
# 4.46.3 alone then broke diffusers (a newer diffusers required
# transformers>=5 APIs, e.g. Dinov2WithRegistersConfig). Pin BOTH together
# to a combination that is actually known to interoperate, not just one.
diffusers==0.31.0
transformers==4.46.3
peft>=0.11
accelerate>=0.30
safetensors>=0.4
onnxruntime>=1.18
pillow>=10.0
